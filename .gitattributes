* text=auto
*.sh text eol=lf
*.ps1 text eol=crlf
*.bat text eol=crlf

# Test and contract scripts are engineering support code rather than Worker runtime.
# Exclude them from GitHub Linguist language statistics so the repository language
# breakdown better reflects the implementation under src/.
scripts/*-test.mjs linguist-detectable=false
