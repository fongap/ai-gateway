name: Reliability Convergence 1.3.5 Pass 2

on:
  push:
    branches: [fix/reliability-convergence-1.3.5]

permissions:
  contents: write

jobs:
  patch:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-24.04
    timeout-minutes: 10

    steps:
      - name: Checkout repair branch
        uses: actions/checkout@v4
        with:
          ref: fix/reliability-convergence-1.3.5
          fetch-depth: 0

      - name: Apply convergence cleanup
        run: python3 scripts/reliability-convergence-135-pass2.py

      - name: Remove one-time repair files
        run: |
          rm scripts/reliability-convergence-135-pass2.py
          rm .github/workflows/reliability-convergence-135-pass2.yml

      - name: Validate patch formatting
        run: git diff --check

      - name: Commit cleanup
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix: remove retired capacity semantics"
          git push origin HEAD:fix/reliability-convergence-1.3.5
