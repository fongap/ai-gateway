fix(P1): 8 critical reliability fixes + workflow contract fixes

## P1-1: Valid response gate
- New EMPTY_200 failure kind for 200 responses with no meaningful output
- 4 native paths (OpenAI Chat/Responses, Anthropic Messages) now gate recordNodeSuccess on meaningful output predicates

## P1-2: Post-header deadline through body assembly
- Added readWithDeadline helper in guard.ts with deadline-aware reader.read() race
- collectOpenAIStreamObject, collectAnthropicMessageObject, collectResponsesObject now honor per-attempt absolute deadline
- 5 call sites in success.ts thread attemptDeadlineMs

## P1-3: Native stream semantic EOF
- track.ts: completionSeen now triggers immediate flush + controller.close() instead of waiting for HTTP EOF
- Prevents idle-timeout misclassification of healthy streams as failures

## P1-4: Cross-protocol commit boundary tightened
- Added conversion-aware predicates: isOpenAIChatRealOutputForConversion, isAnthropicNativeRealOutputForConversion
- Cross-protocol streaming guard now excludes reasoning/thinking (which converters cannot express)

## P1-5: Fallback config error blocking fixed
- nodes.ts: all PROTOCOL_FALLBACKS diagnostics now consistently trip status=invalid AND appear in returned diagnostics
- Removed split logic that caused 'blocks but missing' or 'silently passes'

## P1-6: Tier 2/3 honors explicit Retry-After without jitter
- classify.ts: explicitRetryAfter flag on FailureClassification
- node-state.ts: recordFailure respects flag, skips maybeJitter for explicit provider hints

## P1-7: Tier 1 local admission ceiling
- New maxInFlight policy field (default 4, matching stress-test contract)
- claimTier1Slot now enforces per-account concurrency cap

## P1-8: Observability semantics separated
- track.ts: onUsage fires ONLY for success+completionSeen (not failure/neutral)
- queries.ts: model-status evidence now uses usage_reports>0 instead of requests>0
- observability.ts + gateway-stats.ts: removed client-facing double-wrapper; single trackStreamResponse in makeNodeStreamTrack counts each stream exactly once

## Workflow: Deployment contract fixes
- deployment-workflow-contract-test.mjs: normalize CRLF line endings before parsing
- All 18 contracts now pass (were failing due to CRLF in workflow YAML files)

## Tests updated
- reliability-core-contract-test.mjs: C19 vocabulary includes new EMPTY_200 kind
- stream-completion-test.mjs: 2 test expectations updated for new usage semantics
- token-usage-test.mjs: 1 test expectation updated for new interrupted-stream usage behavior

## CHANGELOG
- Version 1.3.6 (2026-09-14) with all Fixed/Changed entries

Full test:gate passes (49/49 unit suites, 31/31 integration, 18/18 workflow contracts).