{
  "name": "ai-gateway",
  "version": "5.14.0",
  "private": true,
  "description": "Personal AI Agent Node Scheduler. Cloudflare Workers-based AI Gateway with tier-1/tier-2/tier-3 node pools.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run verify",
    "check": "node --check src/index.js",
    "check:version": "node scripts/version-check.mjs",
    "check:links": "node scripts/link-check.mjs",
    "test": "node scripts/config-validation-test.mjs && node scripts/nodes-shard-test.mjs && node scripts/smoke-test.mjs && node scripts/hardening-test.mjs && node scripts/scheduler-test.mjs && node scripts/reliability-test.mjs",
    "test:scheduler": "node scripts/scheduler-test.mjs",
    "test:reliability": "node scripts/reliability-test.mjs",
    "security:scan": "node scripts/secret-scan.mjs",
    "verify": "npm run check && npm run check:version && npm run check:deployment-config && npm run check:links && npm test && npm run security:scan",
    "checksums": "node scripts/generate-checksums.mjs",
    "dev": "node scripts/run-wrangler.mjs dev",
    "deploy": "npx --yes wrangler@4.114.0 deploy --keep-vars",
    "tail": "npx --yes wrangler@4.114.0 tail",
    "cf:login": "npx --yes wrangler@4.114.0 login",
    "cf:whoami": "npx --yes wrangler@4.114.0 whoami",
    "check:deploy": "node scripts/run-wrangler.mjs deploy --dry-run --keep-vars --outdir .wrangler-dry-run",
    "verify:release": "npm run verify && npm run check:deploy",
    "check:deployment-config": "node scripts/deployment-config-check.mjs"
  },
  "keywords": [
    "cloudflare-workers",
    "ai-gateway",
    "openai-api",
    "anthropic-api",
    "claude-code",
    "llm",
    "api-proxy",
    "edge-computing",
    "serverless"
  ]
}
