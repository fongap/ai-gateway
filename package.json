{
  "name": "ai-gateway",
  "version": "1.3.1",
  "private": true,
  "description": "Resilient AI API gateway for Cloudflare Workers — multi-provider routing, multi-key load balancing, rate limiting, tiered failover, and OpenAI/Anthropic compatibility.",
  "type": "module",
  "license": "MIT",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "npm run validate:merge",
    "check": "node scripts/check-syntax.mjs",
    "check:version": "node scripts/version-check.mjs",
    "check:links": "node scripts/link-check.mjs",
    "check:docs": "node scripts/docs-check.mjs",
    "test:unit": "node tests/run-unit.mjs",
    "test:all": "npm run test:unit && node tests/scheduler-stability-test.mjs && node tests/integration-test.mjs && node tests/stress-test.mjs && node tests/codex-contract-test.mjs && node tests/claude-contract-test.mjs",
    "test": "npm run test:unit",
    "test:integration": "node tests/integration-test.mjs",
    "test:conversion": "node tests/conversion-test.mjs",
    "bench": "node benchmark/benchmark.mjs --quick",
    "bench:full": "node benchmark/benchmark.mjs",
    "security:scan": "node scripts/secret-scan.mjs",
    "typecheck": "npx tsc --noEmit",
    "validate:merge": "npm run check && npm run check:version && npm run check:deployment-config && npm run migrations:check && npm run test:unit && npm run security:scan && npm run check:docs && npm run typecheck && npm run check:links",
    "validate:deploy": "npm run check && npm run check:version && npm run check:deployment-config && npm run migrations:check && npm run test:all && npm run security:scan && npm run check:docs && npm run typecheck && npm run check:links",
    "dev": "node scripts/cloudflare-wrangler.mjs dev",
    "deploy": "node scripts/cloudflare-wrangler.mjs deploy --keep-vars",
    "tail": "npx --yes wrangler@4.114.0 tail",
    "cf:login": "npx --yes wrangler@4.114.0 login",
    "cf:whoami": "npx --yes wrangler@4.114.0 whoami",
    "check:deploy": "node scripts/cloudflare-wrangler.mjs deploy --dry-run --keep-vars --outdir .wrangler-dry-run",
    "check:deployment-config": "node scripts/deployment-config-check.mjs",
    "migrations:check": "node scripts/migrations-check.mjs",
    "config:check": "node scripts/config-cli.mjs check",
    "config:show": "node scripts/config-cli.mjs show",
    "config:diff": "node scripts/config-cli.mjs diff",
    "discovery:check": "node scripts/provider-discovery.mjs check-snapshot scripts/provider-discovery/samples/catalog.example.json",
    "discovery:summary": "node scripts/provider-discovery.mjs summary scripts/provider-discovery/samples/catalog.example.json"
  },
  "keywords": [
    "ai-gateway",
    "llm-gateway",
    "llm-router",
    "cloudflare-workers",
    "claude-code",
    "openai-api",
    "openai-compatible",
    "anthropic-api",
    "load-balancing",
    "rate-limiting"
  ],
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
